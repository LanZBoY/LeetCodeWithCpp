#include <iostream>
#include <vector>

using namespace std;

class Solution
{
public:
    int countUnguarded(int m, int n, vector<vector<int>> &guards, vector<vector<int>> &walls)
    {
        int count = 0;
        vector<vector<bool>> gridStatus(m, vector(n, false));

        return count;
    }
};

int main()
{
    int m, n;
    vector<vector<int>> guards;
    vector<vector<int>> walls;
    return 0;
}